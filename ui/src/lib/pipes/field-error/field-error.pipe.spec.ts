import { FieldErrorPipe } from './field-error.pipe';

describe('FieldErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
